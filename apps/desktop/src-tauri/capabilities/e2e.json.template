{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "e2e",
  "description": "Permissions only loaded when the e2e Cargo feature is enabled. Grants the playwright control plane.",
  "platforms": ["macOS"],
  "windows": ["main"],
  "permissions": ["playwright:default"]
}
